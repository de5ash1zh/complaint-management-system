# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/complaint_management
# OR for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/complaint_management

# Email Configuration (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com

# Admin Configuration
ADMIN_EMAIL=admin@yourcompany.com

# Next.js Configuration
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# NextAuth (Authentication)
NEXTAUTH_SECRET=replace-with-a-strong-random-string
# For local dev, NextAuth can infer URL, but it's recommended to set:
NEXTAUTH_URL=http://localhost:3000

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual configuration
# 3. For Gmail, use an App Password instead of your regular password
# 4. Make sure .env.local is in your .gitignore file
